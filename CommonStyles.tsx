import { StyleSheet } from "react-native";

const CommonStyles = StyleSheet.create({
    screenContainer: {
        flex: 1,
        padding: 10,
        justifyContent: "space-evenly",
    },
});

export { CommonStyles };
